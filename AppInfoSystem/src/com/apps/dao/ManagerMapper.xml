<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.apps.dao.ManagerMapper">
	
	<!-- 根据Code 查询 -->
	<select id="getOne" resultType="BackendUser">
		select * from backend_user where userCode=#{userCode}
	</select>
	<!-- 新增开发者用户 -->
	<!-- <insert id="insertDevUser">
		insert into dev_user(devCode,devName)
		values(#{devCode},#{devName})
	</insert> -->
</mapper>